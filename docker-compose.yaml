version: "3.9"

services:
  server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - server-space:/app

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-space:/data

volumes:
  server-space:
  redis-space:
